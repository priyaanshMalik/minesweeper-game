# important utility functions
import settings


def height_prct(percentage):
    return (settings.HEIGHT / 100) * percentage  # that is x percent of HEIGHT


def width_prct(percentage):
    return (settings.WIDTH / 100) * percentage  # that is x percent of Width
